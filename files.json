[     {         "unit_converter.py": "Contains the main business logic for unit conversion. Includes functions to convert length, weight, and temperature units. The main function handles user input and calls the appropriate conversion function."     },     {         "cli.py": "Implements the Typer CLI wrapper that provides a command-line interface for the unit converter. This file will import the conversion functions from unit_converter.py and provide commands for each type of conversion."     } ]
